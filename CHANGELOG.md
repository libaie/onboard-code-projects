# Changelog

## Unreleased

- Add an explicitly authorized, recoverable controller task-set reset that preserves bounded sanitized handoffs while replacing long-lived controller and project entry tasks.
- Harden reset cutover against concurrent candidate creation, reused operation IDs, CHAIN/runtime races, incomplete evidence, and interrupted seal recovery.
- Add guarded initialization and state management for an optional generic controller.
- Add durable controller-task recovery, exact project/task identity rules, and v2 controller results.
- Preserve legacy trusted adapters, resolve junction aliases before overlap checks, and add explicit stale-binding recovery.
- Add bilingual usage, recovery, contribution, and release documentation.
- Replace counterexample-driven controller policy growth with a closed invariant model and an enforced bounded same-CHAIN review/rework/rebaseline cycle.
- Replace the race-prone two-phase goal handshake with one persisted sealed dispatch, canonical task semantics, and evidence-gated at-most-once delivery.
- Reduce runtime-approval churn with safe native defaults, no-continuation waits, and explicit existing-task override handling.
- Add a bounded four-quadrant request intake that proceeds on complete context, limits material questions, challenges weak premises, and converts shared unknowns into minimum experiments.
- Add deterministic closed source parsing, per-source Git options, semantic deduplication, and credential-safe UTF-8 Base64 transport.
- Add generated-controller v2 per-project FIFO dispatch queues, evidence-based model classes, and explicit known-v1 migration with rollback.
- Enforce one three-attempt convergence budget across blocked and completed-but-review-failed attempts, truthful runtime cancellation, foreground-only monitoring, and credential-free controller history.
- Add exact Stop receipts and a quiet per-controller wake transport, with atomic claim/acknowledgement, deterministic automation identity, and a tested installed-runtime rule instead of a global shared writable root.
- Add canonical goal lineages, bounded success/failure experience, material-strategy replay blocking, closed dependency predicates, exact terminal envelopes, and quiescent-only controller runtime upgrades.
- Add evidence-bound historical experience import, dependency-unsatisfied blocking, and one bounded final transport preflight reconciliation.
- Add LF checkout rules, real Node.js version preflight, clean-archive release gating, and executable Skill installer documentation.
- Harden public packaging with an exact file allowlist, fail-closed history scanning, and private-path and credential-shape checks.
- Reject embedded or encoded credential shapes at source and controller-state boundaries, and keep zero-repository replay limited to three documented transport failures.
- Canonicalize filesystem aliases before comparing project and controller roots, including Windows 8.3 paths and migrated registries.
